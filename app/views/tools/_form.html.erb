<%= form_for(Comment.new) do |f| %>

  <div class="field">
    <%= f.text_field :text %>
    <%= f.hidden_field :tool_id, value: params[:id] %>
    <%= f.hidden_field :user_id, value: session[:user_id] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
